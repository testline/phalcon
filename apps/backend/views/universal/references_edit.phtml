<? ob_start(); ?>


<?= $this->tag->formBlockStart($title) ?>

    <?= $form->start() ?>
    <?= $form->render('name', 'Название') ?>
    <?= $form->showSubmitButton() ?>
    <?= $form->end() ?>

<?= $this->tag->formBlockEnd() ?>

<?
$html = ob_get_contents();
ob_end_clean();
die(json_encode(array(
    'html' => $html,
    'title' => $title,
    'breadCrumbs' => $breadCrumbs
)));
?>